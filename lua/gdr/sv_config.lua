tGDRConfig = {
    
    -- address of where the node server is hosted
    -- remember to run srcds with -allowlocalhttp if you're running the node server locally 
    Endpoint = "http://127.0.0.1:1337",

    -- how many times per second will shall query discord messages
    UpdateRate = 1
}